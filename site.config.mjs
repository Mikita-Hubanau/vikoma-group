// ─────────────────────────────────────────────────────────────
//  ГЛАВНЫЙ ФАЙЛ НАСТРОЕК САЙТА
//
//  Домен меняется ТОЛЬКО здесь. Больше нигде в проекте
//  адрес сайта не прописан.
// ─────────────────────────────────────────────────────────────

/** Домен, на котором сайт будет работать в боевом режиме. */
export const DOMAIN = 'vikoma.by';

/**
 * Полный адрес сайта.
 * При сборке для GitHub Pages подменяется переменной SITE_URL,
 * чтобы карта сайта и ссылки в соцсетях вели куда надо.
 */
export const SITE_URL = process.env.SITE_URL || `https://${DOMAIN}`;

/**
 * Подпапка, в которой лежит сайт.
 * На своём домене это корень '/'.
 * На GitHub Pages сайт живёт в подпапке — её подставит сборка.
 */
export const BASE_PATH = process.env.BASE_PATH || '/';
