# Only PUBLIC IDs and URLs. Never add SMTP passwords or private API keys here.
# Empty values use src/content/system/integrations.json as the fallback.
PUBLIC_GOOGLE_ANALYTICS_ID=
PUBLIC_CONTACT_FORM_ENDPOINT=
PUBLIC_REGISTRATION_URL=

# Optional deployment overrides; GitHub Actions sets these automatically.
# SITE_URL=https://vikub.group
# BASE_PATH=/
