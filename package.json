{
  "name": "vikub-group",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "build:map": "node scripts/build-map.mjs",
    "check:cms": "node scripts/check-cms-config.mjs",
    "prebuild": "npm run check:cms && npm run check:site && npm test",
    "check:site": "node scripts/check-site.mjs",
    "check:launch": "node scripts/check-site.mjs --launch",
    "test": "node --test tests/*.test.mjs",
    "postbuild": "node scripts/postbuild.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/source-serif-4": "^5.3.0",
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "d3-geo": "^3.1.1",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2",
    "yaml": "^2.9.0"
  }
}
